@model IEnumerable<ENISNC.Models.DemandePFE>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create"><button type="button" asp-action="Create" class="btn btn-primary">Create New</button></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.demanded_at)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.approved_at)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.demanded_at)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.approved_at)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="  fas fa-edit"></a>
                    <a asp-action="Details" asp-route-id="@item.Id" class=" fas fa-align-justify"></a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class=" fas fa-prescription-bottle"></a>
            </td>
        </tr>
}
    </tbody>
</table>
