@model ENISNC.Models.Departement

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Departement</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.spacialitée)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.spacialitée)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.DepartementId"><button type="button" class="btn btn-primary">Edit</button></a>
    <a asp-action="Index"><button asp-action="Index" type="button" class="btn btn-primary">Back</button></a>
</div>
