@model IEnumerable<ENISNC.Models.Paper>

@{
    ViewData["Title"] = "Index";
}

<h1>Paper Request</h1>
<br />
<br />

<p>
    <a asp-action="Create"><button type="button" asp-action="Create" class="btn btn-primary">Create New</button></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Staus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.demanded_at)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.approved_at)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Staus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.demanded_at)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.approved_at)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.paperId" class="  fas fa-edit"></a>
                    <a asp-action="Details" asp-route-id="@item.paperId" class=" fas fa-align-justify"></a>
                    <a asp-action="Delete" asp-route-id="@item.paperId" class=" fas fa-prescription-bottle"></a>
            </td>
        </tr>
}
    </tbody>
</table>
